#include <stdlib.h>
#include "player.h"
#include "inventory.h"
#include "item.h"

/* void throwRock(Map *map, Player *p, int x, int y) {
    if (p->inventory->nr_of_rocks > 0) {
        p->inventory->nr_of_rocks--;
        p->inventory->nr_items--;
        // TODO add rock to map
    }
} */
